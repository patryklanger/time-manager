import { DateToTimeStringPipe } from './date-to-time-string.pipe';

describe('DateToTimeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToTimeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
